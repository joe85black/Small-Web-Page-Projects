# Balance Sheet

A static, semantic HTML table layout practicing accessible data tables, a sticky column header, and screen-reader-only content (`sr-only`) for a simple company balance sheet (assets, liabilities, net worth).

Open `index.html` in a browser to view.
