# CSS Colored Markers

A small practice project demonstrating CSS color notations (`rgb()`, hex, `hsl()`), linear gradients, and box shadows by drawing three colored marker pens.

Open `index.html` in a browser to view.
