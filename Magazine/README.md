# Magazine

A magazine-style article layout practicing CSS Grid page structure, multi-column text flow (`column-width`), a drop-cap effect with `::first-letter`, generated quote marks via `::before`/`::after`, and responsive breakpoints.

Open `index.html` in a browser to view.
