# Cafe Menu

A static cafe menu page practicing semantic HTML (`section`/`article`), inline-block layout for aligning item names with prices, and basic link and background-image styling.

Open `index.html` in a browser to view.
