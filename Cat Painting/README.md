# Cat Painting

A pure-CSS art project that draws a cat's face using only styled `div` elements, practicing border-triangle tricks, `border-radius`, absolute positioning, and `transform: rotate()` to compose complex shapes.

Open `index.html` in a browser to view.
