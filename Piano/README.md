# Piano

A CSS-only mock piano UI, practicing layout of floated elements and pseudo-elements to fake black keys layered over white keys, plus responsive sizing with media queries.

Open index.html in a browser to view.
