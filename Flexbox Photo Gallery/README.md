# Flexbox Photo Gallery

A responsive image gallery practicing CSS Flexbox: wrapping items into rows, centering with `justify-content`, consistent `gap` spacing, and a `::after` spacer trick to keep the final row aligned.

Open `index.html` in a browser to view.
