# Survey Form

A customer feedback survey form practicing a mix of HTML input types (text, email, number, radio, select, checkboxes, textarea) grouped with fieldsets and labels.

Open index.html in a browser to view.
