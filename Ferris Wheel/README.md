# Ferris Wheel

A pure-CSS animated ferris wheel practicing `@keyframes` animations, `transform-origin` for rotating spokes around a shared center, and counter-rotation so the cabins stay upright while the wheel spins.

Open `index.html` in a browser to view.
