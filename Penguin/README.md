# Penguin

A pure-CSS art project drawing an animated penguin scene, practicing CSS custom properties (`:root` variables), layered absolute positioning, gradients, a waving-arm `@keyframes` animation, and an `:active` interaction state.

Open `index.html` in a browser to view.
