# Registration Form

A user registration form practicing native HTML form validation (required fields, email/password patterns) and grouped inputs with fieldsets, legends, and labels.

Open index.html in a browser to view.
